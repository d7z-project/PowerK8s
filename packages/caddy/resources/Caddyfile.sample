## 在本地搭建 boot.powerk8s.cn Web 服务器提供镜像仓库和 Helm Charts 服务
boot.powerk8s.cn {
    # 安装  powerk8s-tls 软件包后使用 powerk8s-tls server boot.powerk8s.cn  install /var/lib/caddy 安装证书
    tls /etc/caddy/server.cert /etc/caddy/server.key
    reverse_proxy /v2/* 127.0.0.1:8080
    file_server * {
        root /var/www/
        hide .git .*
        browse
        index index.html
        pass_thru
    }
}
